'use strict';

var LinkedList = require('list');

describe('LinkedList', function() {

    var list;

    beforeEach(function () {
        list = new LinkedList();
    });

    xdescribe('Add and remove nodes', function () {
        beforeEach(function (done) {
                done();
        });

        xit('Should have triggered test5 (all) four times', function () {
            //expect(test5).toEqual(4);
        });

    });

});
